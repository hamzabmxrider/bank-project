apiVersion: v1
kind: ConfigMap
metadata:
  name: bank-core-config
  namespace: bank-account
data:
  SERVER_PORT: "8080"
  SPRING_JPA_HIBERNATE_DDL_AUTO: "update"
  SPRING_SQL_INIT_MODE: "never"
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgresql.bank-account.svc.cluster.local:5432/bank_account"
