apiVersion: v1
kind: Service
metadata:
  name: bank-core
  namespace: bank-account
spec:
  selector:
    app: bank-core
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
